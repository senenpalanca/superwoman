import 'package:flutter/material.dart';

class ProjectTile extends StatelessWidget {
  String? name;

  ProjectTile(String name,{Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
